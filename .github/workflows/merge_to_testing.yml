name: Merge-from-master-to-testing
 
on:
  push:
    branches:
      - master
 
jobs:
  merge:
    runs-on: ubuntu-latest
 
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        ref: 'testing'
 
    - name: Merge changes
      id: merge_changes
      run: |
        echo '[========== Run "git status" ===============================]'
        git status
        echo '[========== Run "git pull --rebase origin master" ==========]'
        git pull --rebase origin master
        echo '[========== Run "git push origin testing" ==================]'
        git push origin testing
        echo '[========== Run "git status" ===============================]'
        git statusee

    - name: Send notification [Failed]
      uses: neonidian/teams-notify-build-status@v3
      if: ${{ !success() }}                                                          
      with:
        webhookUrl: ${{ secrets.TEAMS_HOOK }}
        message: >-
          Failed merge into testing. Please click the button for more details =>
        status: Failure
      env:
        SHOULD_DISPLAY_VIEW_RUN_BUTTON: true

    - name: Send notification [Success]
      uses: neonidian/teams-notify-build-status@v3
      with:
        webhookUrl: |
          ${{ secrets.TEAMS_HOOK }}
        title: LANDING_MODULE
        titleBackgroundColor: ${{ steps.merge_changes.outcome }}                     
        status: ${{ steps.merge_changes.outcome }}                                   
        message: >-
          TEST ${{ steps.merge_changes.outcome }}       
      env:
        SHOULD_DISPLAY_ACTOR_LABEL: true
        SHOULD_DISPLAY_VIEW_RUN_BUTTON: true
        SHOULD_DISPLAY_VIEW_COMMIT_BUTTON: true

    - name: Send notification [Always]
      uses: neonidian/teams-notify-build-status@v3
      if: always()
      with:
        webhookUrl: |
         ${{ secrets.TEAMS_HOOK }}
        message: Merge from master to testing has been run
        titleBackgroundColor: ${{ job.status }}
      env:
        SHOULD_DISPLAY_VIEW_COMMIT_BUTTON: true
